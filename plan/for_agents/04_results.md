# Order 04 — Results: docs, changelog, closeout gates

Reader-facing story for the Jacobian round (joint LWA, batched pass, time
variation) plus the round's final gates. No source change beyond the
kinematics package guide; all math already landed in orders 01–03.

## Final gate counts

| Gate | Command | Result |
|------|---------|--------|
| CPU  | `pytest tests/ -q -m "not bench and not cuda"` | **1737 passed**, 61 deselected |
| CUDA | `CUDA_VISIBLE_DEVICES=2 pytest tests/ -q -m cuda` | **60 passed**, 1738 deselected |
| Sphinx (strict) | `BR_DOCS_NITPICKY=1 sphinx-build -W --keep-going -b html` | **exit 0, zero warnings** |
| Doctest | `sphinx-build -b doctest` | **32 tests, 0 failures** (was 31; +1 new LWA example) |
| ruff | `ruff check src/ tests/` | 111 pre-existing errors, **none in a touched file** (my diff clean) |

The sphinx HTML build regenerated the stale autodoc page for
`better_robot.kinematics.jacobian` (autodoc2 `auto_mode`); it was committed
listing 5 symbols while the source already exported 8. No new module files, so
no new API stub pages — all new symbols live in the existing `jacobian.py`.

## Headline bench table (from 02/03 results, GPU batch 256, fp32)

Launches are the whole-pass total (constant, independent of `njoints`);
`/joint` alongside. Timing = median of 30 iters after 5 warmups.

| Model   | njoints | nv  | launches before | /joint | launches after | fwd before | fwd after |
|---------|---------|-----|-----------------|--------|----------------|------------|-----------|
| smpl25  | 26      | 78  | 1324            | 50.9   | 52             | 14.204 ms  | 0.614 ms  |
| smplx56 | 57      | 171 | 2967            | 52.1   | 52             | 39.338 ms  | 0.778 ms  |
| mhr203  | 68      | 204 | 3550            | 52.2   | 52             | 47.349 ms  | 0.842 ms  |
| panda   | 14      | 8   | 486             | 34.7   | 52             | 7.392 ms   | 0.480 ms  |

Batched Jacobian pass: ~50 launches/joint → constant 52 whole-pass; mhr203
forward 47.3 → 0.84 ms (56×). CPU Panda (batch 1) also improves, no regression
(fwd 2.29 → 0.13 ms).

Combined J + J̇ pass (mhr203-like, njoints 68, nv 204):

| Pass                                     | launches | /joint |
|------------------------------------------|----------|--------|
| J alone (`joint_jacobians_raw`)          | 52       | 0.76   |
| combined J+J̇ (`..._time_variation_raw`) | 60       | 0.88   |
| incremental J̇                           | 8        | —      |

J̇ adds a constant 8 launches (einsum for `ov`, owner gather, two cross pairs,
cat, transpose, second mask-multiply, second mimic reduce), independent of
`njoints`.

## Per-doc-file summary

- `docs/concepts/kinematics_and_jacobians.md` — one sentence added to the
  reference-frames section (`get_joint_jacobian` now takes all three references,
  default `"world"`); new "How a Jacobian changes in time" section: what `J̇`
  is, the three frames, the prominent acceleration pairing (WORLD/LOCAL ↔
  spatial, LWA ↔ classical, because the LWA basis point moves), transport-only
  in one sentence, and one runnable `{testcode}`/`{testoutput}` example checking
  `a = J v̇ + J̇ v` for the LWA frame (classical) against a finite difference —
  prints `True` (fp32, two-joint builder arm, max err ~1.6e-5).
- `docs/concepts/design_decisions.md` — `(decision-jacobian-frame)` extended
  with the `J̇` default-frame choice (joints `"world"`, frames
  `"local_world_aligned"`, mirroring the static getters) and the single-WORLD-
  cache decision (derive LOCAL/LWA in the getter vs a per-frame cache).
- `src/better_robot/kinematics/CLAUDE.md` — Entry Points gain the five new
  functions with signatures; Jacobian Reference Frames section notes joint LWA
  exists and carries the `J̇` acceleration-pairing warning; new "Jacobian Hot
  Path" paragraph (batched shared-column pass, `_joint_jacobian_plan` memo,
  transport-only `J̇` at +8 launches), mirroring "FK Hot Path".
- `docs/CHANGELOG.md` — one new Unreleased bullet at the top: batched Jacobian
  pass (~50/joint → constant 52; 203-joint model forward 47.3 → 0.84 ms), joint
  `local_world_aligned`, and the `J̇` API for joints + frames in three frames
  with the acceleration pairing and the +8-launch cost.
- `docs/reference/roadmap.md` — "What is implemented" Jacobian line now reads
  "batched analytic spatial Jacobians and their time variation for joints and
  frames in all three references".
- `docs/reference/api/better_robot/better_robot.kinematics.jacobian.md` —
  regenerated by the strict Sphinx build; now lists
  `JointJacobiansTimeVariationResult`, `joint_jacobians_time_variation_raw`,
  `frame_jacobian_time_variation_raw`, `compute_joint_jacobians_time_variation`,
  `get_joint_jacobian_time_variation`, `get_frame_jacobian_time_variation`.

## Notes

- No `MIGRATION.md` change: the round is purely additive.
- No src change beyond `kinematics/CLAUDE.md`; no order-01..03 docstring xref
  fix was needed (strict nitpicky build resolves every cross-reference).
