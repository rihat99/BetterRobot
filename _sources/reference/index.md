# Reference

Authoritative, factual material — everything you might need to look up
while working with BetterRobot.

```{toctree}
:maxdepth: 2

api/better_robot/better_robot
named_block_solvers
changelog
roadmap
m1_removed_symbols
m3_removed_symbols
glossary
```

## What lives where

| Page | Contents |
|------|----------|
| {doc}`api/better_robot/better_robot` | Auto-generated symbol reference, built from the docstrings under `src/better_robot/`. |
| {doc}`named_block_solvers` | Public named-block LM/GN lifecycle, state, and status API. |
| {doc}`changelog` | Per-release notes. |
| {doc}`roadmap` | What is named in the public surface but still stubbed in `src/`. |
| {doc}`m1_removed_symbols` | Downstream migration ledger for surfaces removed in M1. |
| {doc}`m3_removed_symbols` | Downstream migration ledger for surfaces removed in M3.5. |
| {doc}`glossary` | Vocabulary, storage layouts, and the renames inherited from Pinocchio. |
