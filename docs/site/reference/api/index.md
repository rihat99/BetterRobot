# API reference

The auto-generated symbol-by-symbol reference is built from the
docstrings in `src/better_robot/`. Submodules:

```{toctree}
:maxdepth: 1

top_level
lie
kinematics
dynamics
optim
tasks
```
