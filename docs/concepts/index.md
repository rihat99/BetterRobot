# Concepts

Explanations of *why* BetterRobot is shaped the way it is. Read these
when you want to understand the architecture rather than just call the
API.

```{toctree}
:maxdepth: 1

model_and_data
lie_and_spatial
batched_by_default
backends
solver_stack
```

For the full normative specs, see {doc}`/design/index` and
{doc}`/conventions/index`.
